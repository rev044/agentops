# Goals Tests

Validates GOALS.md (and legacy GOALS.yaml) schema integrity and fitness function correctness. Checks that goal definitions parse correctly, required fields are present, fitness measurements produce expected results, and directive management operations remain consistent. Supports both Markdown and YAML goal file formats.

## Running

```bash
bash tests/goals/validate-goals.sh
```
