# plan

Plan work for Codex with explicit execution boundaries, issue-ready decomposition, and handoff artifacts that feed the rest of the RPI chain cleanly.

## Codex Execution Profile

1. Treat `skills/plan/SKILL.md` as the canonical planning contract and `skills-codex/plan/SKILL.md` as the Codex-facing artifact.
2. Convert research output into beads-ready issues, dependencies, and acceptance criteria without drifting into implementation.
3. Write plan artifacts so `$crank`, `$rpi`, and future Codex sessions can pick them up without chat-only context.

## Guardrails

1. Keep WHAT and HOW separated; Codex plans should be execution-ready, not speculative design sprawl.
2. Prefer precise issue titles, dependency edges, and validation criteria over long narrative plans.
3. When Codex-specific wording or orchestration differs from Claude, update this override instead of hand-editing generated output.
