---
id: pre-mortem-2026-04-12-wiki-formalization
type: pre-mortem
date: 2026-04-12
source: "[[.agents/plans/2026-04-12-agents-wiki-formalization]]"
prediction_ids:
  - pm-20260412-001
  - pm-20260412-002
  - pm-20260412-003
---

# Pre-Mortem: Wiki Formalization

## Council Verdict: WARN

| ID | Finding | Severity | Prediction |
|----|---------|----------|------------|
| pm-20260412-001 | Missing overnight.go output path update | significant | Forge tier1 in forge.go is updated but the Dream post-loop hook in overnight.go:507 still writes to ao/sessions/. Sessions forged during overnight will land in the old path. |
| pm-20260412-002 | .gitignore concern for .agents/wiki/ | moderate | .agents/ is in .gitignore in some repos. Need to verify .agents/wiki/ content is trackable or explicitly force-add. Prior session showed .agents/ files need `git add -f`. |
| pm-20260412-003 | INDEX.md staleness without auto-refresh | moderate | INDEX.md is generated by a script but nothing auto-runs it. After forge/harvest adds new pages, INDEX.md is stale until someone runs the script manually. Should hook into SessionEnd or forge post-write. |

## Pseudocode Fixes

```
Finding: pm-20260412-001 — overnight.go output path not in plan
Severity: significant
Fix:
  In cli/cmd/ao/overnight.go:507, change:
    outDir := filepath.Join(cwd, ".agents", "ao", "sessions")
  To:
    outDir := filepath.Join(cwd, ".agents", "wiki", "sources")
  Also update dream_subcycle.go if it has the same path.
Affected files: cli/cmd/ao/overnight.go, cli/cmd/ao/dream_subcycle.go
```

```
Finding: pm-20260412-003 — INDEX.md auto-refresh
Severity: moderate
Fix:
  After WriteSessionPage succeeds in forge_tier1.go, call:
    exec.Command("bash", "scripts/generate-index.sh", agentsDir).Run()
  Or: append a single line to INDEX.md inline (cheaper, no script dependency).
  Preferred: inline append in AppendToLog helper — same write path, no extra process.
Affected files: cli/internal/llm/forge_tier1.go or scripts/generate-index.sh
```

## Shared Findings
- Plan is well-scoped (6 issues, 3 waves, ~4 hours)
- Backward compat is preserved (old ao/sessions/ stays, search scans both)
- Low blast radius: 2 Go files change (search.go + forge.go), rest is new files

## FAIL Pattern Check
1. Missing mechanical verification — PASS (conformance checks on every issue)
2. Self-assessment — PASS (go build + go test verify)
3. Context rot — N/A (single-session scope)
4. Propagation blindness — **WARN** (overnight.go output path missed in plan)
5. Plan oscillation — PASS (direction is clear)
6. Dead infrastructure — PASS (wiki/ has immediate consumers via ao search)
7. Missing rollback — PASS (additive changes, no rollback needed)
8. Four-surface closure — WARN (no docs update planned, but this IS the docs layer)

## Recommendation

**PROCEED with fixes.** Add overnight.go output path change to Issue 5. Consider inline INDEX.md append in the AppendToLog helper instead of a separate refresh script. The plan is solid — small scope, clear value, no risky changes.

## Decision Gate

[x] PROCEED - Council passed with 2 actionable fixes applied inline
