# v2.15.0 Release Notes

## Highlights

- **cmd/ao coverage crosses 60%** after 5 rounds of targeted test writing
- **116 evolve cycles landed** with zero complexity violations and 95%+ coverage across all packages
- **Repo root cleaned up** -- docs moved to `docs/` for a leaner project layout
- **CI fully green** after fixing embedded sync, security scan false positives, and toolchain gate

## What's New

- Test coverage for `cmd/ao` pushed from 51.6% to 60.2% through 5 rounds of targeted tests covering 30+ helper functions across badge, store, fire, batch_promote, rpi_phased, and context modules
- Vibe-kit selection manifest added for plugin packaging
- CI badges and Codecov integration with complexity and race detection pre-push gates
- Infrastructure scripts enhanced with `--per-file`, `--track-delta`, and cycle estimator

## All Changes

### Added

- cmd/ao test coverage to 60.2% (5 rounds, 30+ helper functions)
- Vibe-kit selection manifest for plugin packaging
- CI badges, Codecov integration, complexity + race pre-push gates
- Infrastructure scripts with --per-file, --track-delta, and cycle estimator

### Changed

- Repo root decluttered -- CHANGELOG, CONTRIBUTING, CODE_OF_CONDUCT, SECURITY moved to docs/
- 116 evolve cycles -- zero complexity >= 8, 95%+ coverage, sentinel errors, benchmarks, Go 1.23 idioms
- CI hardened -- embedded sync, security scan, toolchain gate fixes
- Git author corrected across 157 commits

### Fixed

- CI pipeline (embedded sync path, secretFile false positive, toolchain gate)
- Push-gate $ROOT race condition
- Race test mitigations (-count=1, timeout, duration monitoring)
- Local CI gate (test file exclusion, --require-tools removal)
- Golangci-lint debt (dead code, errcheck, gosimple)
- 4 failing goals (telemetry, doc links, GOALS path, evolve measurement)

[Full changelog](../../docs/CHANGELOG.md)
