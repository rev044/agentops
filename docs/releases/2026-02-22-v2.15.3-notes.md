# v2.15.3 — Stale reference cleanup

Follows v2.15.2 (hooks strip). Removes stale references to removed hook features across skills and docs.

## Highlights

- **Skills fixed** — `/post-mortem` no longer calls removed `ao feedback-loop` or `ao session-outcome`; `/learn` flywheel diagram updated
- **Docs cleaned** — Removed dead env vars (PRECOMPACT_DISABLED, TASK_VALIDATION_DISABLED, WORKER) from ENV-VARS.md
- **Preflight fixed** — Removed dead `batch-feedback` check from hook validation script
- **Skill counts synced** — 52 total (42 user-facing, 10 internal) across all manifests
