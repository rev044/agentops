# v2.19.1 — Quickstart hotfix + swarm quality wave

**Highlights:**

- **`/quickstart` is fast again** — 275 → 68 lines, 3+ min → under 30 sec. The skill was generating a 90-line ASCII diagram and 50-line intent router on every invocation. Now it runs one bash block and outputs ~8 lines.

- **Spec-consistency gate** — `scripts/spec-consistency-gate.sh` validates contract files before crank spawns workers, catching spec drift before it burns worker tokens.

- **Unicode-safe truncation** — 6 truncation functions were doing byte-based slicing. All converted to rune-safe slicing, preventing corruption on multi-byte characters.

**All 15 commits since v2.19.0 are included.**
